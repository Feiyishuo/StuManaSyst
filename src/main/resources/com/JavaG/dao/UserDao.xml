<?xml version="1.0" encoding="UTF-8"?>
<!--与数据库交互，查表语句-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.JavaG.dao.UserDao">

    <select id="getUser" parameterType="String"  resultType="com.JavaG.domain.User" >
    select * from user_login where username = #{username} and password=#{password}
    </select>
    <select id="getUserByName" parameterType="String"  resultType="com.JavaG.domain.User" >
    select * from user_login where username = #{username}
    </select>
    <update id="updatePass">
        update user_login set password=#{newPass} where id=#{username}
    </update>
    <update id="updateUserStatus">
       update user_login set status=#{status} where id=#{id}
    </update>
    <select id="getAllUsers" resultType="com.JavaG.domain.Student">
        select a.*,b.username,b.status from student a,user_login b where a.id=b.id
    </select>

</mapper>